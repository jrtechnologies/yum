import { TinymceDirective } from './tinymce.directive';

describe('TinymceDirective', () => {
  it('should create an instance', () => {
    const directive = new TinymceDirective();
    expect(directive).toBeTruthy();
  });
});
